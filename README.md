# ai-hub-media
Public media assets for a personal AI Intelligence Hub

## Loop player

`/player/?src=<HTTPS_MP4_URL>` provides a muted, inline, autoplaying loop player for Notion embeds. It pauses off-screen and keeps native controls as a fallback when autoplay is blocked.
